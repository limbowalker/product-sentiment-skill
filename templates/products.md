# Product(s)

List the product name(s) you want feedback on — one per line. That's the only
required setup. The AI uses these names to search and to stay on-scope.

Optionally add a short scope note (one line) if the name is ambiguous or shared
with other products, so the AI knows what to include/exclude.

<!-- NOTES-START (ignored by the AI) — your private reminders go here. NOTES-END -->

## Products

- Your Product Name (Platform / Edition)

## Scope note (optional)

- Describe what to include/exclude so the AI stays on-scope (e.g. only the
  official product by publisher X; exclude third-party tools that share the name).
